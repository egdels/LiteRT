# Sonatype Central Portal credentials
# DO NOT commit real values here.
# Store in ~/.gradle/gradle.properties or use environment variables.
#
# sonatypeUsername=<your-central-portal-token-username>
# sonatypePassword=<your-central-portal-token-password>
#
# GPG signing (option 1: local GPG agent — no config needed here)
# GPG signing (option 2: inline key for CI — set via env vars)
#   GPG_KEY_ID, GPG_PRIVATE_KEY, GPG_PASSPHRASE

org.gradle.parallel=false
org.gradle.caching=false
